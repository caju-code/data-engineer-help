[project]
name = "queimadas-api"
version = "0.1.0"
description = "API Flask para consulta de focos de queimadas no Databricks"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "databricks-sql-connector[pyarrow]>=4.2.5",
    "flask>=3.1.3",
    "gunicorn>=23.0.0",
    "python-dotenv>=1.2.2",
]
